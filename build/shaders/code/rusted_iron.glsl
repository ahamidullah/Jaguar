// This file was auto-generated by material_compiler.c.

// Shader blocks:
//    flat_color
//    world_space

#version 420

#if defined(VERTEX_SHADER)

layout (location = 0) in vec3 vertex_position; // world_space

layout (location = 0) out vec4 fragment_color; // flat_color

layout (set = 0, binding = 0) uniform Bind_Per_Material_Update_Delayed_UBO {
	vec4 color; // flat_color
};

layout (set = 1, binding = 0) uniform Bind_Per_Object_Update_Immediate_UBO {
	mat4 model_to_world_space; // world_space
};

void main() {
	// flat_color
	{
		fragment_color = vec4(1.0, 0.0, 0.0, 1.0);
	}

	// world_space
	{
		//gl_Position = model_to_world_space * vec4(vertex_position, 1.0);
		gl_Position = vec4(vertex_position, 1.0);
	}
}

#endif


#if defined(FRAGMENT_SHADER)

layout (location = 0) in vec4 fragment_color; // flat_color

layout (location = 0) out vec4 output_color; // flat_color

void main() {
	// flat_color
	{
		output_color = fragment_color;
	}
}

#endif


